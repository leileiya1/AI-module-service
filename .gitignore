# ----------------------------
# 只保留：每个模块下的 pom.xml 和 src/**（以及 .gitignore 本身）
# 其余一律忽略
# ----------------------------

# 默认忽略所有文件
*

# 允许目录遍历（否则不能递归到子目录）
!*/

# 保留 .gitignore 本身
!.gitignore

# 保留根 pom（如果有）
!/pom.xml

# 保留任何子目录中的 pom.xml
!*/pom.xml

# 保留当前目录下的 src/ 及其所有内容（若仓库根直接就是模块）
!src/
!src/**

# 保留任意子目录下的 src/ 及其所有内容（模块通常在子目录）
!*/src/
!*/src/**

# 可选：显式忽略常见的构建产物和 IDE 配置（防止误放入）
# （这些行可保留或者删除 — 因为上面已经 * 默认忽略所有 *）
target/
**/target/
*.iml
.idea/
.vscode/
*.class
*.log
.mvn/